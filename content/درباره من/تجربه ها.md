---
title: تجربه
draft: false
image: "[[تجربه ها.png]]"
tags:
  - درباره
  - معرفی
order: "4"
---

<div class="timeline-experience-wrapper">


  <style>
    /* ==========================================================================
       DESIGN SYSTEM & VARIABLES (Apple / Minimal Luxury - Full Width Glass Cards)
       ========================================================================== */
    .timeline-experience-wrapper {
      --font-family: Vazirmatn, "SF Pro Display", -apple-system, BlinkMacSystemFont, Tahoma, sans-serif;
      --bg-gradient: radial-gradient(circle at 50% 0%, rgba(120, 119, 198, 0.08) 0%, rgba(255, 255, 255, 0) 70%);
      --card-bg: rgba(255, 255, 255, 0.65);
      --card-border: rgba(255, 255, 255, 0.7);
      --card-shadow: 0 20px 40px -15px rgba(0, 0, 0, 0.05), 0 0 15px rgba(0, 0, 0, 0.02);
      --card-shadow-hover: 0 30px 60px -20px rgba(0, 0, 0, 0.12), 0 0 20px rgba(0, 0, 0, 0.04);
      --glass-blur: blur(20px) saturate(180%);
      --text-main: #1d1d1f;
      --text-muted: #6e6e73;
      --accent: #0071e3;
      --accent-soft: rgba(0, 113, 227, 0.08);
      --line-color: rgba(0, 0, 0, 0.08);
      --line-progress: linear-gradient(180deg, #0071e3 0%, #9858f5 100%);
      --radius-lg: 24px;
      --radius-md: 14px;
      --radius-sm: 8px;
      --transition-smooth: cubic-bezier(0.16, 1, 0.3, 1);
      font-family: var(--font-family);
      color: var(--text-main);
      position: relative;
      padding: 20px 0 80px; /* حذف پدینگ اضافی چپ و راست برای پر شدن کامل عرض */
      width: 100%;
      max-width: 100%;
      margin: 0 auto;
      background: var(--bg-gradient);
      box-sizing: border-box;
      direction: rtl;
      text-align: right;
    }
    /* Dark Mode Auto-Adaptation */
    @media (prefers-color-scheme: dark) {
      .timeline-experience-wrapper {
        --card-bg: rgba(28, 28, 30, 0.6);
        --card-border: rgba(255, 255, 255, 0.12);
        --card-shadow: 0 20px 40px -15px rgba(0, 0, 0, 0.5);
        --card-shadow-hover: 0 30px 60px -20px rgba(0, 0, 0, 0.7);
        --text-main: #f5f5f7;
        --text-muted: #86868b;
        --line-color: rgba(255, 255, 255, 0.12);
      }
    }
    .timeline-experience-wrapper * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }
    /* Header Styling */
    .timeline-header {
      text-align: center;
      margin-bottom: 60px;
      padding: 0 20px;
    }
    .timeline-header h1 {
      font-size: 2.4rem;
      font-weight: 700;
      letter-spacing: -0.02em;
      margin-bottom: 12px;
      background: linear-gradient(180deg, var(--text-main) 0%, var(--text-muted) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    .timeline-header p {
      font-size: 1.05rem;
      color: var(--text-muted);
      font-weight: 400;
    }
    /* Timeline Container Structure */
    .timeline-container {
      position: relative;
      width: 100%;
      margin: 0 auto;
    }
    .timeline-track {
      position: absolute;
      top: 0;
      bottom: 0;
      right: 50%;
      width: 2px;
      background: var(--line-color);
      transform: translateX(50%);
      z-index: 1;
    }
    .timeline-progress {
      position: absolute;
      top: 0;
      right: 0;
      width: 100%;
      height: 0%;
      background: var(--line-progress);
      transition: height 0.1s linear;
      border-radius: 2px;
    }
    /* Timeline Items (Full Fill Layout) */
    .timeline-item {
      position: relative;
      display: flex;
      justify-content: flex-start;
      padding-bottom: 60px;
      padding-left: 35px; /* فاصله از خط وسط */
      padding-right: 0;
      width: 50%;
      opacity: 0;
      transform: translateY(40px);
      transition: opacity 0.8s var(--transition-smooth), transform 0.8s var(--transition-smooth);
    }
    .timeline-item:nth-child(even) {
      align-self: flex-end;
      margin-right: auto;
      justify-content: flex-end;
      padding-right: 35px; /* فاصله از خط وسط */
      padding-left: 0;
    }
    .timeline-item.is-visible {
      opacity: 1;
      transform: translateY(0);
    }
    /* Center Dot Accent */
    .timeline-dot {
      position: absolute;
      top: 32px;
      left: -8px;
      width: 16px;
      height: 16px;
      border-radius: 50%;
      background: var(--text-main);
      border: 3px solid rgba(255, 255, 255, 0.9);
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.15);
      z-index: 2;
      transition: transform 0.4s var(--transition-smooth);
      transform: scale(0.6);
    }
    .timeline-item:nth-child(even) .timeline-dot {
      left: auto;
      right: -8px;
    }
    .timeline-item.is-visible .timeline-dot {
      transform: scale(1);
    }
    /* Glassmorphism Card Architecture - Full Stretch */
    .timeline-card {
      width: 100%; /* استفاده از تمام عرض دسترس */
      background: var(--card-bg);
      backdrop-filter: var(--glass-blur);
      -webkit-backdrop-filter: var(--glass-blur);
      border: 1px solid var(--card-border);
      border-radius: var(--radius-lg);
      padding: 32px;
      box-shadow: var(--card-shadow);
      transition: transform 0.4s var(--transition-smooth), box-shadow 0.4s var(--transition-smooth);
      position: relative;
      overflow: hidden;
    }
    .timeline-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--card-shadow-hover);
    }
    /* Card Header */
    .card-header {
      display: flex;
      align-items: center;
      gap: 16px;
      margin-bottom: 16px;
    }
    .company-logo {
      width: 48px;
      height: 48px;
      border-radius: var(--radius-md);
      background: linear-gradient(135deg, rgba(0,0,0,0.04), rgba(0,0,0,0.01));
      border: 1px solid var(--card-border);
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 700;
      font-size: 0.85rem;
      color: var(--text-main);
      flex-shrink: 0;
    }
    .card-title-group h2 {
      font-size: 1.25rem;
      font-weight: 700;
      line-height: 1.4;
      color: var(--text-main);
    }
    .card-company {
      font-size: 0.9rem;
      color: var(--text-muted);
      font-weight: 500;
    }
    .card-date {
      display: inline-block;
      font-size: 0.8rem;
      font-weight: 600;
      color: var(--accent);
      background: var(--accent-soft);
      padding: 4px 12px;
      border-radius: var(--radius-sm);
      margin-bottom: 16px;
    }
    .card-description {
      font-size: 0.95rem;
      line-height: 1.7;
      color: var(--text-main);
      opacity: 0.9;
      margin-bottom: 18px;
    }
    /* Achievements List */
    .card-achievements {
      list-style: none;
      margin-bottom: 20px;
    }
    .card-achievements li {
      position: relative;
      padding-right: 18px;
      font-size: 0.88rem;
      color: var(--text-muted);
      line-height: 1.6;
      margin-bottom: 8px;
    }
    .card-achievements li::before {
      content: "•";
      position: absolute;
      right: 0;
      color: var(--accent);
      font-size: 1.2rem;
      line-height: 1;
    }
    /* Skill Tags */
    .card-tags {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-bottom: 16px;
    }
    .tag {
      font-size: 0.75rem;
      font-weight: 500;
      padding: 5px 12px;
      border-radius: var(--radius-sm);
      background: rgba(0, 0, 0, 0.03);
      border: 1px solid rgba(0, 0, 0, 0.05);
      color: var(--text-main);
      transition: all 0.3s var(--transition-smooth);
    }
    @media (prefers-color-scheme: dark) {
      .tag {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.08);
      }
    }
    .tag:hover {
      background: var(--accent);
      color: #ffffff;
      border-color: var(--accent);
      transform: translateY(-2px);
    }
    /* Accordion Details Section */
    .card-details {
      max-height: 0;
      overflow: hidden;
      opacity: 0;
      transition: max-height 0.5s var(--transition-smooth), opacity 0.4s ease;
    }
    .timeline-card.is-expanded .card-details {
      max-height: 600px;
      opacity: 1;
      margin-top: 20px;
      padding-top: 20px;
      border-top: 1px dashed var(--line-color);
    }
    .gallery-grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 12px;
      margin-bottom: 18px;
    }
    .gallery-item {
      aspect-ratio: 16 / 9;
      border-radius: var(--radius-md);
      background: linear-gradient(135deg, rgba(0, 0, 0, 0.04), rgba(0, 0, 0, 0.01));
      border: 1px solid var(--card-border);
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.8rem;
      color: var(--text-muted);
    }
    .learned-section {
      background: var(--accent-soft);
      border-right: 3px solid var(--accent);
      padding: 12px 16px;
      border-radius: var(--radius-sm) 0 0 var(--radius-sm);
      font-size: 0.88rem;
      color: var(--text-main);
      line-height: 1.6;
    }
    .learned-section strong {
      display: block;
      font-size: 0.78rem;
      font-weight: 700;
      color: var(--accent);
      margin-bottom: 4px;
    }
    /* Expand Button */
    .expand-btn {
      width: 100%;
      background: none;
      border: none;
      padding: 12px 0 0;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      font-size: 0.85rem;
      font-weight: 600;
      font-family: inherit;
      color: var(--text-muted);
      cursor: pointer;
      transition: color 0.3s ease;
    }
    .expand-btn:hover {
      color: var(--accent);
    }
    .chevron {
      width: 16px;
      height: 16px;
      fill: none;
      stroke: currentColor;
      stroke-width: 2;
      stroke-linecap: round;
      stroke-linejoin: round;
      transition: transform 0.4s var(--transition-smooth);
    }
    .timeline-card.is-expanded .chevron {
      transform: rotate(180deg);
    }
    /* Accessibility / Reduced Motion */
    @media (prefers-reduced-motion: reduce) {
      .timeline-item, .timeline-card, .chevron, .tag, .card-details {
        transition: none !important;
      }
    }
    /* Mobile & Tablet Responsiveness */
    @media (max-width: 868px) {
      .timeline-track {
        right: 20px;
        transform: none;
      }
      .timeline-item,
      .timeline-item:nth-child(even) {
        width: 100%;
        right: 0;
        justify-content: flex-start;
        padding-right: 45px;
        padding-left: 0;
        padding-bottom: 40px;
      }
      .timeline-dot,
      .timeline-item:nth-child(even) .timeline-dot {
        right: 12px;
        left: auto;
      }
      .timeline-card {
        width: 100%;
      }
    }
  </style>

  <header class="timeline-header">
    <h1>مسیر تجارب حرفه‌ای</h1>
    <p>طراحی معماری، خلق پروژه‌های دیجیتال و توسعه دانش تخصصی</p>
  </header>

  <div class="timeline-container">
    <div class="timeline-track">
      <div class="timeline-progress" id="timelineProgress"></div>
    </div>
    <!-- ITEM 1: HOZ Engineering -->
    <article class="timeline-item">
      <div class="timeline-dot"></div>
      <div class="timeline-card">
        <div class="card-header">
          <div class="company-logo">HOZ</div>
          <div class="card-title-group">
            <h2>طراح و ویژوالایزر فضا‌های درمانی</h2>
            <div class="card-company">مهندسین مشاور HOZ</div>
          </div>
        </div>
        <span class="card-date">۱۴۰۲ — تاکنون</span>
        <p class="card-description">
          مدیریت و اجرای سیستم‌های بصری‌سازی سه‌بعدی و طراحی معماری مراکز درمانی تخصصی با تمرکز بر ارگونومی، سیرکولاسیون استاندارد و زیبایی‌شناسی مدرن ساختمانی.
        </p>
        <ul class="card-achievements">
          <li>طراحی سناریوهای بصری‌سازی سه‌بعدی و نمای ورودی بیمارستان دکتر فرزان اردکان.</li>
          <li>تلفیق هویت معماری بومی الگوی آجرکاری با استانداردهای مدرن ورودی بیمارستانی.</li>
        </ul>
        <div class="card-tags">
          <span class="tag">معماری درمانی</span>
          <span class="tag">مدلسازی سه‌بعدی</span>
          <span class="tag">طراحی نما</span>
          <span class="tag">برنامه‌ریزی فضایی</span>
        </div>
        <div class="card-details" id="details-1">
          <div class="gallery-grid">
            <div class="gallery-item">مفهوم بصری ورودی بیمارستان</div>
            <div class="gallery-item">جزئیات رندر آجرکاری نما</div>
          </div>
          <div class="learned-section">
            <strong>دست‌آورد کلیدی</strong>
            ایجاد تعادل میان ضوابط سخت‌گیرانه فضا‌های درمانی و هویت معماری بومی نیازمند دقت بالا و کنترل جزئیات ریز در مقیاس ساختمانی است.
          </div>
        </div>
        <button class="expand-btn" aria-expanded="false" aria-controls="details-1">
          <span class="btn-text">مشاهده جزئیات</span>
          <svg class="chevron" viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"></polyline></svg>
        </button>
      </div>
    </article>
    <!-- ITEM 2: Faradars -->
    <article class="timeline-item">
      <div class="timeline-dot"></div>
      <div class="timeline-card">
        <div class="card-header">
          <div class="company-logo">فرادرسی</div>
          <div class="card-title-group">
            <h2>مدرس و متخصص تولید محتوا</h2>
            <div class="card-company">فرادرس (Faradars)</div>
          </div>
        </div>
        <span class="card-date">۱۴۰۱ — تاکنون</span>
        <p class="card-description">
          تالیف و تدریس دوره‌های آموزشی تخصصی مهندسی و نرم‌افزارهای کاربردی برای مهندسان و طراحان در یکی از بزرگ‌ترین پلتفرم‌های آموزش آنلاین کشور.
        </p>
        <ul class="card-achievements">
          <li>طراحی و ضبط سرفصل‌های آموزشی جامعی برای نرم‌افزارهای تخصصی حوزه مهندسی.</li>
          <li>تدوین استراتژی‌های کمپین‌های تبلیغاتی برای افزایش بازدهی و مخاطبان دوره‌ها.</li>
        </ul>
        <div class="card-tags">
          <span class="tag">آموزش تخصصی</span>
          <span class="tag">تدوین سرفصل</span>
          <span class="tag">بازاریابی محتوایی</span>
        </div>
        <div class="card-details" id="details-2">
          <div class="gallery-grid">
            <div class="gallery-item">نمای دوره آموزشی</div>
            <div class="gallery-item">تحلیل آمار و بازخورد دوره‌ها</div>
          </div>
          <div class="learned-section">
            <strong>دست‌آورد کلیدی</strong>
            سادگی و شفافیت در انتقال مفاهیم پیچیده مهندسی وابسته به ساختار داستان‌گویی گام‌به‌گام و راهنمای بصری دقیق است.
          </div>
        </div>
        <button class="expand-btn" aria-expanded="false" aria-controls="details-2">
          <span class="btn-text">مشاهده جزئیات</span>
          <svg class="chevron" viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"></polyline></svg>
        </button>
      </div>
    </article>
    <!-- ITEM 3: Healthcare Research -->
    <article class="timeline-item">
      <div class="timeline-dot"></div>
      <div class="timeline-card">
        <div class="card-header">
          <div class="company-logo">پژوهش</div>
          <div class="card-title-group">
            <h2>پژوهشگر ارشد معماری سلامت</h2>
            <div class="card-company">پروژه تحقیقاتی فضا‌های درمانی</div>
          </div>
        </div>
        <span class="card-date">۱۴۰۰ — ۱۴۰۱</span>
        <p class="card-description">
          انجام پژوهش‌های تخصصی پیرامون روانشناسی محیط و تاثير نور طبیعی و هندسه فضایی بر کاهش استرس بیماران در کادر‌های درمانی و کلینیک‌ها.
        </p>
        <ul class="card-achievements">
          <li>تدوین راهنماهای متدولوژی طراحی کانسپت ورودی‌های کلینیکی.</li>
          <li>بررسی دقیق استانداردهای ساخت‌وساز مطابق با مباحث مقررات ملی ساختمان.</li>
        </ul>
        <div class="card-tags">
          <span class="tag">روانشناسی محیطی</span>
          <span class="tag">مقررات ملی ساختمان</span>
          <span class="tag">پژوهش کاربردی</span>
        </div>
        <div class="card-details" id="details-3">
          <div class="gallery-grid">
            <div class="gallery-item">دیاگرام تحلیل نورگیری</div>
            <div class="gallery-item">نمودار رفتار حرکتی بیمار</div>
          </div>
          <div class="learned-section">
            <strong>دست‌آورد کلیدی</strong>
            طراحی انسان‌محور در محیط‌های پراسترس، مستقیماً روند بهبود کادر درمانی و مراجعین را تسریع می‌بخشد.
          </div>
        </div>
        <button class="expand-btn" aria-expanded="false" aria-controls="details-3">
          <span class="btn-text">مشاهده جزئیات</span>
          <svg class="chevron" viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"></polyline></svg>
        </button>
      </div>
    </article>
    <!-- ITEM 4: PODBM Podcast -->
    <article class="timeline-item">
      <div class="timeline-dot"></div>
      <div class="timeline-card">
        <div class="card-header">
          <div class="company-logo">پادکست</div>
          <div class="card-title-group">
            <h2>سازنده و میزبان</h2>
            <div class="card-company">پادکست PODBM</div>
          </div>
        </div>
        <span class="card-date">۱۳۹۹ — ۱۴۰۰</span>
        <p class="card-description">
          تولید و کارگردانی سلسله اپیزودهای صوتی با محوریت متدولوژی‌های طراحی، تفکر دیزاین، و ارتباطات حرفه‌ای در کسب‌وکار.
        </p>
        <ul class="card-achievements">
          <li>تولید محتوای صوتی با رویکرد لحن حمایتی و توسعه فردی.</li>
          <li>جذب و رشد ارگانیک مخاطبان فعال از طریق ارائه‌ی ارزش و محتوای کاربردی.</li>
        </ul>
        <div class="card-tags">
          <span class="tag">تولید پادکست</span>
          <span class="tag">داستان‌گویی</span>
          <span class="tag">توسعه هویت برند</span>
        </div>
        <div class="card-details" id="details-4">
          <div class="gallery-grid">
            <div class="gallery-item">استودیو ضبط پادکست</div>
            <div class="gallery-item">کاور گرافیکی اپیزودها</div>
          </div>
          <div class="learned-section">
            <strong>دست‌آورد کلیدی</strong>
            گوش دادن فعال و تغییر لحن از حالت تهاجمی به حمایتی، اعتماد عمیق‌تر و پایدارتری در مخاطب ایجاد می‌کند.
          </div>
        </div>
        <button class="expand-btn" aria-expanded="false" aria-controls="details-4">
          <span class="btn-text">مشاهده جزئیات</span>
          <svg class="chevron" viewBox="0 0 24 24"><polyline points="6 9 12 15 18 9"></polyline></svg>
        </button>
      </div>
    </article>
  </div>

  <script>
    (function () {
      'use strict';

      document.addEventListener('DOMContentLoaded', function () {
        const timelineWrapper = document.querySelector('.timeline-experience-wrapper');
        if (!timelineWrapper) return;

        const items = timelineWrapper.querySelectorAll('.timeline-item');
        const progressLine = document.getElementById('timelineProgress');

        /* 1. IntersectionObserver reveal animation */
        const observerOptions = {
          root: null,
          rootMargin: '0px 0px -10% 0px',
          threshold: 0.15
        };

        const itemObserver = new IntersectionObserver((entries) => {
          entries.forEach(entry => {
            if (entry.isIntersecting) {
              entry.target.classList.add('is-visible');
            }
          });
        }, observerOptions);

        items.forEach(item => itemObserver.observe(item));

        /* 2. Scroll Progress Line Calculation */
        function updateTimelineProgress() {
          if (!progressLine) return;

          const wrapperRect = timelineWrapper.getBoundingClientRect();
          const windowHeight = window.innerHeight;
          
          const totalHeight = wrapperRect.height;
          const currentScroll = windowHeight / 2 - wrapperRect.top;
          
          let progressPercentage = (currentScroll / totalHeight) * 100;
          progressPercentage = Math.max(0, Math.min(100, progressPercentage));
          
          progressLine.style.height = progressPercentage + '%';
        }

        let ticking = false;
        window.addEventListener('scroll', function () {
          if (!ticking) {
            window.requestAnimationFrame(function () {
              updateTimelineProgress();
              ticking = false;
            });
            ticking = true;
          }
        });
        updateTimelineProgress();

        /* 3. Accordion Interactive Toggle */
        const expandButtons = timelineWrapper.querySelectorAll('.expand-btn');

        expandButtons.forEach(button => {
          button.addEventListener('click', function () {
            const card = this.closest('.timeline-card');
            const isExpanded = card.classList.contains('is-expanded');
            const btnText = this.querySelector('.btn-text');

            if (isExpanded) {
              card.classList.remove('is-expanded');
              this.setAttribute('aria-expanded', 'false');
              if (btnText) btnText.textContent = 'مشاهده جزئیات';
            } else {
              card.classList.add('is-expanded');
              this.setAttribute('aria-expanded', 'true');
              if (btnText) btnText.textContent = 'بستن جزئیات';
            }
          });
        });
      });
    })();
  </script>
</div>