---
title: مهارت‌ها
draft: false
image: "[[مهارت ها.png]]"
tags:
  - درباره
  - معرفی
order: "5"
---
 

<div class="skills-radar-wrap">
    <svg
        id="skillsRadar"
        viewBox="0 0 400 380"
        preserveAspectRatio="xMidYMid meet">
    </svg>
</div>

<style>

.radar-fill,
.radar-stroke{
    transition: .35s ease;
}

#skillsRadar:hover .radar-fill{
    fill-opacity: .38;
}

#skillsRadar:hover .radar-stroke{
    stroke-width: 3;
    filter: drop-shadow(0 0 10px var(--secondary));
}
.radar-dot{
 پcursor:pointer;

 transition:.25s;
}

.radar-dot:hover{
 r:6;
 filter:drop-shadow(0 0 8px var(--secondary));
}
.skills-radar-wrap{

    --radar-grid: var(--lightgray);

    --radar-fill: var(--secondary);

    --radar-stroke: var(--secondary);

    --radar-text: var(--dark);

    --radar-point: var(--secondary);

}
.skills-radar-wrap{

    width:100%;

    max-width:700px;

    margin:auto;

    display:flex;

    justify-content:center;

    align-items:center;

    padding:25px 0;

}

#skillsRadar{

    width:100%;

    height:auto;

    display:block;

}

#skillsRadar text{

    font-family:Tahoma,"Vazirmatn",sans-serif;

    font-size:14px;
    fill:var(--dark);
    transition:.3s;

}

/* Tablet */

@media(max-width:900px){

#skillsRadar text{

font-size:12px;

}

}

/* Mobile */

@media(max-width:650px){

.skills-radar-wrap{

padding:10px;

}

#skillsRadar text{

font-size:10px;

font-weight:600;

}

}

/* Small Mobile */

@media(max-width:420px){

#skillsRadar text{

font-size:8px;

}

}

.radar-dot{
cursor:pointer;
transition:all .25s ease;
}

.radar-dot:hover{
r:6;
filter:drop-shadow(0 0 8px var(--secondary));
}

.radar-label{
transition:.25s;
cursor:default;
}

.radar-label:hover{
fill:var(--secondary);
font-weight:700;
}

#skillsRadar{
transition:.35s ease;
}

#skillsRadar:hover{
transform:scale(1.02);
}

#skillsRadar:hover path{
transition:.35s;
}

#skillsRadar:hover line{
opacity:.85;
}
</style>

<script>

const style = getComputedStyle(
    document.querySelector(".skills-radar-wrap")
);
const GRID = style.getPropertyValue("--radar-grid").trim();

const TEXT = style.getPropertyValue("--radar-text").trim();

const FILL = style.getPropertyValue("--radar-fill").trim();

const STROKE = style.getPropertyValue("--radar-stroke").trim();

const POINT = style.getPropertyValue("--radar-point").trim();

(function(){
  // ---------- داده‌های مهارت‌ها را از اینجا تغییر بده (بین 0 تا 100) ----------
  const skills = [
    { label: "Autocad",   value: 85 },
    { label: "Sketchup",         value: 75 },
    { label: "Lumion", value: 70 },
    { label: "Revit",     value: 60 },
    { label: "3d max + corona",   value: 20 },
    { label: "Enscape",      value: 80 },
  ];
  // -----------------------------------------------------------------------

  const svg = document.getElementById('skillsRadar');
  const cx = 200, cy = 190;      // مرکز چارت
  const maxR =
 window.innerWidth < 420 ? 95 :
 window.innerWidth < 600 ? 110 :
 window.innerWidth < 900 ? 125 :
 140;             // شعاع بیرونی‌ترین شش‌ضلعی
  const levels = 4;              // تعداد شش‌ضلعی‌های گرید
  const n = skills.length;
  const angleStep = (Math.PI * 2) / n;

  // زاویه هر محور را طوری تنظیم می‌کنیم که راس بالا باشد (مثل عکس نمونه)
  function pointFor(index, radius){
    const angle = -Math.PI/2 + index * angleStep;
    return {
      x: cx + radius * Math.cos(angle),
      y: cy + radius * Math.sin(angle)
    };
  }

  function hexPath(radius){
    let d = "";
    for(let i=0;i<n;i++){
      const p = pointFor(i, radius);
      d += (i===0 ? "M" : "L") + p.x.toFixed(2) + "," + p.y.toFixed(2) + " ";
    }
    return d + "Z";
  }

  let svgContent = "";

  // 1) گرید شش‌ضلعی‌های تودرتو
  for(let l=1; l<=levels; l++){
    const r = (maxR / levels) * l;
    svgContent += `<path d="${hexPath(r)}" fill="none" stroke="${GRID}" stroke-width="1" opacity="0.6"/>`;
  }

  // 2) خطوط شعاعی از مرکز به هر راس
  for(let i=0;i<n;i++){
    const p = pointFor(i, maxR);
    svgContent += `<line x1="${cx}" y1="${cy}" x2="${p.x.toFixed(2)}" y2="${p.y.toFixed(2)}" stroke="${GRID}" stroke-width="1" opacity="0.6"/>`;
  }

  // 3) چندضلعی مقدار مهارت‌ها (پر شده)
  let dataPath = "";
  skills.forEach((s, i) => {
    const r = (s.value/100) * maxR;
    const p = pointFor(i, r);
    dataPath += (i===0 ? "M" : "L") + p.x.toFixed(2) + "," + p.y.toFixed(2) + " ";
  });
  dataPath += "Z";

  svgContent += `<path d="${dataPath}" fill="${FILL}" fill-opacity="0.25" stroke="${STROKE}" stroke-width="2"/>`;

  // 4) نقطه روی هر راس داده
  skills.forEach((s, i) => {
    const r = (s.value/100) * maxR;
    const p = pointFor(i, r);
    svgContent += `<circle class="radar-dot" cx="${p.x.toFixed(2)}" cy="${p.y.toFixed(2)}" r="4" fill="${POINT}" stroke="white" stroke-width="2"/>`;
  });

  // 5) لیبل‌ها بیرون از شش‌ضلعی
  skills.forEach((s, i) => {
   const labelOffset =
 window.innerWidth < 480 ? 15 :
 window.innerWidth < 768 ? 20 :
 26;
const p = pointFor(i, maxR + labelOffset);;
    let anchor = "middle";
    if (p.x > cx + 5) anchor = "start";
    else if (p.x < cx - 5) anchor = "end";
    svgContent += `<text class="radar-label" x="${p.x.toFixed(2)}" y="${p.y.toFixed(2)}" text-anchor="${anchor}" dominant-baseline="middle" font-weight="600">${s.label}</text>`;
  });

  svg.innerHTML = svgContent;
})();
</script>
